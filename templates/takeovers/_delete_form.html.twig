<form method="post" action="{{ path('app_takeovers_delete', {'id': takeover.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ takeover.id) }}">
    <button class="btn">Delete</button>
</form>
