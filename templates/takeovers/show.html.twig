{% extends 'base.html.twig' %}

{% block title %}Takeovers{% endblock %}

{% block body %}
    <h1>Takeovers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ takeover.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ takeover.Name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ takeover.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_takeovers_index') }}">back to list</a>

    <a href="{{ path('app_takeovers_edit', {'id': takeover.id}) }}">edit</a>

    {{ include('takeovers/_delete_form.html.twig') }}
{% endblock %}
