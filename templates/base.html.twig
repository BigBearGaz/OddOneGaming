<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}OddOne Gaming{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-image: url('/images/OddOne_Gaming_CastleTheme_jpg.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            min-height: 100vh;
        }

        /* Overlay */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.4);
            z-index: -1;
        }

        /* Navigation */
        nav.navbar {
            background: rgba(0, 0, 0, 0.7) !important;
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(0, 255, 0, 0.3);
        }

        .navbar-brand img {
            height: 80px;
        }

        .navbar-nav .nav-link {
            color: #00ff00 !important;
            font-weight: bold;
            text-transform: uppercase;
            font-size: 1.1rem;
        }

        .navbar-nav .nav-link:hover {
            color: #ffffff !important;
            text-shadow: 0 0 10px #00ff00;
        }

        /* Dropdown Menu Styling */
        .dropdown-menu {
            background: rgba(0, 0, 0, 0.9) !important;
            backdrop-filter: blur(15px);
            border: 1px solid rgba(0, 255, 0, 0.3);
            box-shadow: 0 10px 30px rgba(0, 255, 0, 0.2);
            max-height: 400px;
            overflow-y: auto;
        }

        .dropdown-item {
            color: #00ff00 !important;
            font-weight: 600;
            padding: 12px 20px;
            transition: all 0.3s ease;
        }

        .dropdown-item:hover {
            background: rgba(0, 255, 0, 0.2) !important;
            color: #ffffff !important;
            text-shadow: 0 0 10px #00ff00;
            transform: translateX(5px);
        }

        .dropdown-divider {
            border-color: rgba(0, 255, 0, 0.2);
        }

        /* Scrollbar pour le dropdown */
        .dropdown-menu::-webkit-scrollbar {
            width: 8px;
        }

        .dropdown-menu::-webkit-scrollbar-track {
            background: rgba(0, 0, 0, 0.5);
        }

        .dropdown-menu::-webkit-scrollbar-thumb {
            background: rgba(0, 255, 0, 0.5);
            border-radius: 4px;
        }

        .dropdown-menu::-webkit-scrollbar-thumb:hover {
            background: rgba(0, 255, 0, 0.7);
        }

        {% block stylesheets %}{% endblock %}
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="{{ path('app_home') }}">
                <img src="{{ asset('images/OddOneGaming_Logo.png') }}" alt="OddOne Gaming Logo">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_heroes_index') }}">Heroes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_takeovers_index') }}">Takeover</a>
                    </li>
                    
                   {# DROPDOWN DUNGEONS #}
<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="dungeonsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        Dungeons
    </a>
    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dungeonsDropdown">
        <li>
            <a class="dropdown-item" href="{{ path('app_dungeons_index') }}">
                🏰 All Dungeons
            </a>
        </li>
        
        {% set all_dungeons = get_all_dungeons() %}
        {% if all_dungeons|length > 0 %}
            <li><hr class="dropdown-divider"></li>
            {% for dungeon in all_dungeons %}
                <li>
                    <a class="dropdown-item" href="{{ path('app_dungeons_show', {'id': dungeon.id}) }}">
                        🗝️ {{ dungeon.name }}
                    </a>
                </li>
            {% endfor %}
        {% endif %}
    </ul>
</li>


                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_contact') }}">Contact</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_effects') }}">Effects</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_sets') }}">Sets</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container-fluid p-0">
        {% block body %}{% endblock %}
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>
