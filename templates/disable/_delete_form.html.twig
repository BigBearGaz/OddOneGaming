<form method="post" action="{{ path('app_disable_delete', {'id': disable.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ disable.id) }}">
    <button class="btn">Delete</button>
</form>
