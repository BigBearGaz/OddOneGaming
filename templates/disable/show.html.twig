{% extends 'base.html.twig' %}

{% block title %}Disable{% endblock %}

{% block body %}
    <h1>Disable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ disable.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ disable.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ disable.type }}</td>
            </tr>
            <tr>
                <th>IconUrl</th>
                <td>{{ disable.iconUrl }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ disable.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ disable.createdAt ? disable.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_disable_index') }}">back to list</a>

    <a href="{{ path('app_disable_edit', {'id': disable.id}) }}">edit</a>

    {{ include('disable/_delete_form.html.twig') }}
{% endblock %}
