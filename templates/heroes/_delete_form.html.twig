<form method="post" action="{{ path('app_heroes_delete', {'id': hero.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce héros ? Cette action est irréversible.');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hero.id) }}">
    <button class="btn btn-danger w-100">
        <i class="bi bi-trash3-fill"></i> Supprimer
    </button>
</form>
