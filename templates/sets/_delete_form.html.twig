<form method="post" action="{{ path('app_sets_delete', {'id': set.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce set ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ set.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
