{% extends 'base.html.twig' %}

{% block title %}Instants{% endblock %}

{% block body %}
    <h1>Instants</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ instant.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ instant.name }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ instant.label }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ instant.description }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ instant.icon }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ instant.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_instants_index') }}">back to list</a>

    <a href="{{ path('app_instants_edit', {'id': instant.id}) }}">edit</a>

    {{ include('instants/_delete_form.html.twig') }}
{% endblock %}
