<form method="post" action="{{ path('app_buffs_delete', {'id': buff.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ buff.id) }}">
    <button class="btn">Delete</button>
</form>
